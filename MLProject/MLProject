name: mushroom_classification_svm_ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: str, default: "mushroom_preprocessing"}
      cv_folds: {type: int, default: 5}
    command: "python modelling.py --data_dir {data_dir} --cv_folds {cv_folds}"
